# Frontend Architecture

There is no frontend in the current project scope. This placeholder exists because `AGENTS.md` requires a frontend architecture section. If the project later gains a web UI or desktop UI, document it here.
